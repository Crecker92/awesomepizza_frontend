# AwesomePizza

## Install & Run

Launch command `npm i` to install and `npm start` to serve the project locally.
Server environment variables are located in file `environment.ts`.

## Run Tests

Launch command `npm test` to run the test suite.